repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.1
    hooks:
      - id: ruff
        args: [--fix]
        files: ^(src/|tests/)
      - id: ruff-format
        files: ^(src/|tests/)
  - repo: local
    hooks:
      - id: mypy
        name: mypy type check
        entry: bash -c './venv/bin/python -m mypy src/timeblock --check-untyped-defs'
        language: system
        pass_filenames: false
        files: ^src/timeblock/
        stages: [pre-push]
      - id: pytest-all
        name: pytest full suite (unit + integration + bdd + e2e)
        entry: bash -c './venv/bin/python -m pytest tests/ -q --tb=short'
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]
